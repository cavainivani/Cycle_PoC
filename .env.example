# 데이터 소스 선택
#   local = 브라우저 localStorage (기본값, DB 없이 동작 · 지금 단계)
#   rest  = server/index.js 의 /api 사용 (실제 DB 연동 후)
VITE_DATA_SOURCE=local

# rest 를 쓸 때 API 기본 경로. 같은 서버에서 서빙하면 그대로 두면 된다.
# VITE_API_BASE=/api
